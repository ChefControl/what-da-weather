# Rust
/backend/target

# Frontend
/frontend/node_modules
/frontend/dist

# Local environment overrides (committed template: .env.example)
.env

# Model weights are downloaded at runtime into a Docker volume, never committed
*.gguf

.DS_Store
